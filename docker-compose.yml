services:
  frontend:
    build: .
    container_name: thangals-frontend
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - HOSTNAME=0.0.0.0
      - NODE_ENV=development
    command: npm run dev
